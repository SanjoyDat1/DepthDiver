# LensWander Pro — Apple SHARP (ml-sharp) + Anthropic Claude + Gradio
# For NVIDIA CUDA, install CUDA PyTorch before this file if you want GPU speed:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
gradio>=5.0.0
pillow>=10.0.0
python-dotenv>=1.0.0
anthropic>=0.40.0
sharp @ git+https://github.com/apple/ml-sharp.git
